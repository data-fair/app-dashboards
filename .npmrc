# npm is the package manager for this repo (package-lock.json).
# The pnpm-only keys (shamefully-hoist, strict-peer-dependencies) were
# removed: npm warns "Unknown project config" about them on every run.
